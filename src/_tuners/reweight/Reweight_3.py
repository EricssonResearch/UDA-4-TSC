from _tuners.reweight.base import Reweight
from _utils.enumerations import *


class Reweight_3(Reweight):
    def __init__(self):
        raise NotImplementedError
        # super().__init__(n_components=3, reweight_name=TLTunerEnum.Reweight_3)
