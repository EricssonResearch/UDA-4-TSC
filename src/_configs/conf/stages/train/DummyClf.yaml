defaults:
  - _self_
  - optional config: DummyClf/${stages/preprocess}

classifier_name: DummyClf
config: {}
tune_config:
  search_method_name: None
  train_tune_option: tune_configs_only
no_shuffle_split_config:
  n_splits: 1
  test_size: 0.1