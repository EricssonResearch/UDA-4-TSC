dataset_name: sportsActivities
dataset_config:
  source:
    name: p1
  target:
    name: p2
preprocessing_pipeline:
- preprocessor: ZNormalizer
  config:
    verbose: true
- preprocessor: ToPyArrow
  config: {}